apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: bomc-publish-test

bases:
- ./../../base/

commonLabels:
  environment: test

images:
- name: localhost:5000/bomc/publish
  newTag: 1.0.0-68-ga508b98.dirty

patchesStrategicMerge:
- ingress-host.yaml
- app-config-map.yaml
- wait-for-consumer-pod.yaml
- namespace-test.yaml

patchesJson6902:
- path: deployment-config-patch.json
  target:
    # Get this configuration from 030-dc.yaml
    group: apps
    kind: Deployment
    name: publisher
    version: v1
    
# Run it with: kubectl apply -k ./overlay/prod
