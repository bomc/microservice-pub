apiVersion: v1
kind: Service
metadata:
  name: publish-service-nodeport-ingress
  namespace: OVERRIDE_BY_OVERLAYS
  labels:
    app: publish
spec:
  selector:
    app: publish
  ports:
  - protocol: TCP
    name: 8181-tcp
    port: 8181
    targetPort: 8181
  type: NodePort
