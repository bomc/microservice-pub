apiVersion: v1
kind: Service
metadata:
  name: publish-service
  labels:
    app: publish
  namespace: bomc-publish
spec:
  selector:
    app: publish
  ports:
  - protocol: TCP
    name: 8181-tcp
    port: 8181
    targetPort: 8181